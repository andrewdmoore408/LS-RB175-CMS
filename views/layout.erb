<!DOCTYPE html>
<html>

<head>
  <title>ADM CMS</title>
  <meta charset="utf-8">
  <link rel="icon" href="data:,">
</head>

<body>
  <header class="flash-message">
    <% if session[:success] %>
      <div class="flash success">
        <p><%= session.delete(:success) %></p>
      </div>
    <% elsif session[:error] %>
      <div class="flash error">
        <p><%= session.delete(:error) %></p>
      </div>
    <% end %>
  </header>

  <main>
    <%= yield %>
  </main>

</body>
</html>